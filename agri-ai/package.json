{
  "name": "agri-ai",
  "private": true,
  "description": "AgriClimate AI monorepo — one command to run frontend + backend",
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "test": "bash scripts/run-tests.sh",
    "test:live": "bash -c 'export RUN_LIVE=1 && bash scripts/run-tests.sh'",
    "smoke": "bash scripts/smoke-test.sh"
  }
}
